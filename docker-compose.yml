version: '3.8'

services:
  nextjs-client:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    env_file:
      - .env
      - .env.local
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
